soundcard>=0.4.5
numpy>=2.2.3
silero-vad[onnx-cpu]
openai
rich
fastapi
uvicorn[standard]
av
# torch: install manually per pytorch.org/get-started (CUDA version-specific)
qwen-asr
faster-whisper
huggingface_hub[hf_xet]
py-cpuinfo
cutlet
unidic        # full dictionary (needs `python -m unidic download`); cutlet prefers it for accuracy
unidic-lite   # fallback if the full dict was never downloaded, so romanization never hard-fails
pypinyin
anyascii
korean-romanizer
