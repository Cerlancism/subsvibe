# Project memory index

- `./.claude/memory/known-issues/live-render.md` — live render known-issues
  backlog (open + closed), covering `./client/render.py` + `./client/pipeline.py`
  emit logic.
- `./.claude/memory/romanization.md` — romaji/pinyin/RR display gauge: why cutlet
  over pykakasi, and why the JA exceptions list is exactly 2 entries (token-keyed
  hook → only single-token particle mis-sounds are safe to override).
